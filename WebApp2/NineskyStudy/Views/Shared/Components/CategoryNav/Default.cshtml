@model System.Linq.IQueryable<Category>
<div class="navbar-collapse collapse">
    <ul class="nav navbar-nav">
        <li><a asp-controller="Home" asp-action="Index">首页</a></li>
        @foreach (var category in Model)
        {
            <li>
                <a href="@Url.Action("Index", "Category",new {id=category.CategoryId })" target="@category.Target">@category.Name</a>
            </li>
        }
    </ul>
</div>
